# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Env — secrets never committed
.env
.env.*
!.env.example

# Exports produits par l'app (PDF registre, CSV Foncia) : des sorties, pas des
# sources. On en dépose à la racine pour juger un rendu — ils n'ont rien à faire
# dans l'historique.
registre-CS-*.pdf
decision-*.pdf
budgets-CS-Rives-*.csv

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
# Métadonnées macOS (dossier synchronisé) : ._nom-du-fichier
._*
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
